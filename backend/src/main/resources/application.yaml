spring:
  application:
    name: talentiq-backend

  datasource:
    url: jdbc:mysql://localhost:3306/talentiq
    username: root
    password: SMK@09062007
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true